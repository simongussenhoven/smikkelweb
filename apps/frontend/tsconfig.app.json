// this config is not ok but nuxt is not working with tsconfig.json
{
  "extends": [
    "../../tsconfig.base.json"
  ],
  "compilerOptions": {
    "composite": true,
    "noEmit": false,
    "paths": {
      "@stores/*": [
        "src/stores"
      ],
      "@types":
        ["../../types"]
    }
  },
  "exclude": [],
  "include": [
    "src/**/*.ts",
    "src/**/*.vue",
    "../../types",
    "../../public/images/**/*"
  ]
}